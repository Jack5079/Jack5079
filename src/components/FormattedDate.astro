---
const { date, style } = Astro.props
---

<time datetime={date.toISOString()} class="dt-published" {style}>
	{
		date.toLocaleDateString("en-us", {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
