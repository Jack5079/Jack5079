export interface Input {
	value: string
	author: string
}
figure data-nosnippet itemscope itemtype="https://schema.org/Quotation"
	blockquote itemprop="text"
		p -- ${input.value}
	figcaption
		-- ―
		span itemprop="citation" -- ${input.author}
style
	--
	figure {
		margin-inline: 0;
	}
	--
